[[plugins]]
package = "@netlify/plugin-sitemap"

[build.environment]
	# Prevent Netlify secrets scan from flagging known local env keys or build output paths
	# Note: omitting paths/keys only suppresses the scanner — rotate any leaked secrets.
	SECRETS_SCAN_OMIT_PATHS = "build/static/js/**,public/**"
	SECRETS_SCAN_OMIT_KEYS = "REACT_APP_EMAIL_API_ENDPOINT,REACT_APP_API_KEY"

# Uncomment the below when due to go live to public
# Some other things will need to be set up within Sentry.
# If you've not done this, then ask for help.
# [[plugins]]
# package = "@sentry/netlify-build-plugin"

# [plugins.inputs]
#   sentryOrg = "wmca-digital"
#   sentryProject = "wmn-disrapp-name"